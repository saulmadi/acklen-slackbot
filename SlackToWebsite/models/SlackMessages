var mongoose = require('mongoose');

var SlackMessageSchema = new mongoose.Schema({
    ChannelName: String,
    Author: String,
    SlackMessage: String,
    DateTime: Date
});

mongoose.model('SlackMessage', SlackMessageSchema);